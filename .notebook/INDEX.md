# Notebook index

- `start-work-flow.md` — where `/start-work` is handled and why the command router path can skip Fighter.
- `release-boundary-changeset-flow.md` — how `.changeset/generate-from-commits.ts` finds the last release boundary and why semver-sorted tags were wrong.
