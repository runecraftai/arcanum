import { defineCommand } from "citty";
import { outro } from "@clack/prompts";

export default defineCommand({
	meta: {
		name: "install",
		description: "Install agent skills",
	},
	run() {
		outro("⏳ Not yet implemented. Coming soon.");
	},
});
