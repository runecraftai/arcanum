# @runecraft/runes

## 0.2.0

### Minor Changes

- refactor(runes): remove comments and add AGENTS.md no-comments rule
  feat(runes): add node-target build script and plugin integration test
  feat(runes): add runes CLI bin (search, stats, doctor)
  feat(runes): wire plugin entry and tool registry
  feat(runes): add using-runes always-active skill
  feat(runes): add rune_session_start and rune_session_end tools
  feat(runes): add rune_context, rune_timeline, rune_stats tools
  feat(runes): add rune_search, rune_get, rune_update, rune_delete tools
  feat(runes): add rune_save tool
  feat(runes): add config loader with hierarchical merge
  feat(runes): add sqlite schema, migrations, and repository
  feat(runes): add data dir and project slug resolution
