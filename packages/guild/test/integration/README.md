Integration tests cover plugin bootstrap, config loading, and manager wiring without a full host runtime.

Keep tests here when they validate assembled plugin behavior from fixtures and direct plugin entrypoints.
