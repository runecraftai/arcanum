# Phase 6 Spec: Verification and Closure

## Objective

Verify the full feature after builtin skills, RPG identity, `.specs/*` artifact guidance and Guild rebrand are implemented.

## Requirements

- [ ] Run `bun test` in `packages/guild`
- [ ] Run `bun run typecheck` in `packages/guild`
- [ ] Run relevant schema/build checks if touched
- [ ] Search residual `Weave|weave|.weave`
- [ ] Search obsolete artifact guidance like `.weave/plans` and `.guild/plans`
- [ ] Update `.specs/project/STATE.md` with final status and exceptions

## Verification Evidence

Record:

- command run
- pass/fail status
- known environmental failures
- residual exception list
- follow-up work, if any
